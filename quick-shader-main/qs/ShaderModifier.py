# ShaderModifier Functionality (10 Methods)
# adjust_brightness(value: float) → Increases or decreases brightness based on a given factor.
# adjust_contrast(value: float) → Enhances or reduces contrast using a scale factor.
# change_light_direction(new_direction: tuple) → Manually sets a new light direction.
# invert_light_direction() → Flips the detected light direction (useful for negative lighting effects).
# normalize_gradients() → Ensures gradients are within a valid range for shader processing.
# smooth_edges(strength: float) → Applies a smoothing filter to reduce sharp edges.
# sharpen_texture(strength: float) → Enhances texture sharpness for better shader details.
# clamp_values(min_val: float, max_val: float) → Limits all extracted values within a safe shader range.
# auto_adjust_shader_properties() → Automatically modifies values based on best shader practices.
# get_modified_data() → Returns the modified shader attributes as a dictionary.
